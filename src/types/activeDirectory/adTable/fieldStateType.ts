type FieldStateType = {
  login: { used: boolean; ascFlag: boolean };
  firstName: { used: boolean; ascFlag: boolean };
  lastName: { used: boolean; ascFlag: boolean };
  email: { used: boolean; ascFlag: boolean };
  isActive: { used: boolean; ascFlag: boolean };
};
export default FieldStateType;
